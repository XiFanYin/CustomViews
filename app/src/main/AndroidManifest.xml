<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="view.local.zz.customviews">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.AnimationButtonActivity" />
        <activity android:name=".activity.CircleProgressActivity" />
        <activity android:name=".activity.HorizontalProgressActivity" />
        <activity android:name=".activity.BezuerLineActivity" />
        <activity android:name=".activity.CircleBezierProgressActivity" />
        <activity android:name=".activity.BitmapViewActivity" />
        <activity android:name=".activity.MyViewGroup1Activity" />
        <activity android:name=".activity.OpenGLImageActivity" />
        <activity android:name=".activity.OpenGLVBOImageActivity" />
        <activity android:name=".activity.OpenGLFBOImageActivity" />
        <activity android:name=".activity.OpenGlWaterImageActivity" />
        <activity android:name=".activity.OpenGlWaterFBOImageActivity" />
        <activity android:name=".activity.OpenGLCameraActivity" />
        <activity android:name=".activity.OpenGLVideoActivity" />

    </application>

</manifest>